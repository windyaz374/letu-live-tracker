# 🎯 Getting Started - Visual Guide

## Step-by-Step Setup (Windows)

```
┌─────────────────────────────────────────────────────────────┐
│ STEP 1: Install Prerequisites (One-time)                    │
└─────────────────────────────────────────────────────────────┘

    1. Download Python 3.8+
       https://www.python.org/downloads/
       
       ⚠️ IMPORTANT: Check ✅ "Add Python to PATH"
       
    2. Download Node.js 16+ LTS
       https://nodejs.org/
       
    3. Download Chrome Browser
       https://www.google.com/chrome/

┌─────────────────────────────────────────────────────────────┐
│ STEP 2: Google Sheets Setup (One-time)                      │
└─────────────────────────────────────────────────────────────┘

    1. Go to: https://console.cloud.google.com/
    
    2. Create New Project
       ┌─────────────────┐
       │ + New Project   │
       │ Name: Letu      │
       │ [Create]        │
       └─────────────────┘
    
    3. Enable Google Sheets API
       ┌──────────────────────────┐
       │ APIs & Services          │
       │ → Library                │
       │ → Search "Google Sheets" │
       │ → Enable                 │
       └──────────────────────────┘
    
    4. Create Credentials
       ┌────────────────────────────┐
       │ Credentials                │
       │ → + CREATE CREDENTIALS     │
       │ → OAuth client ID          │
       │ → Desktop app              │
       │ → Download JSON            │
       └────────────────────────────┘
    
    5. Save as credentials.json
       Move to: letu-live-tracker\backend\

┌─────────────────────────────────────────────────────────────┐
│ STEP 3: First-Time Setup (Automatic)                        │
└─────────────────────────────────────────────────────────────┘

    1. Double-click: setup.bat
       
       This will:
       ✓ Check Python & Node.js
       ✓ Create virtual environment
       ✓ Install dependencies
       ✓ Configure environment

┌─────────────────────────────────────────────────────────────┐
│ STEP 4: Run the Application                                 │
└─────────────────────────────────────────────────────────────┘

    1. Double-click: start.bat
       
       Two windows open:
       ┌──────────────────┐  ┌──────────────────┐
       │ Backend Server   │  │ Frontend Server  │
       │ Port 5000        │  │ Port 5173        │
       └──────────────────┘  └──────────────────┘
    
    2. Browser opens automatically
       → http://localhost:5173

┌─────────────────────────────────────────────────────────────┐
│ STEP 5: Start Tracking                                      │
└─────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────┐
    │ 📊 Letu Live Tracker                       │
    ├────────────────────────────────────────────┤
    │                                            │
    │ Session ID                                 │
    │ ┌────────────────────────────────────┐    │
    │ │ 29060044                           │    │
    │ └────────────────────────────────────┘    │
    │                                            │
    │ Google Sheet URL                           │
    │ ┌────────────────────────────────────┐    │
    │ │ https://docs.google.com/...        │    │
    │ └────────────────────────────────────┘    │
    │                                            │
    │ [ 👁️ Preview Data ]  [ ▶️ Start Tracking ] │
    │                                            │
    └────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ STEP 6: First-Time Google Authentication                    │
└─────────────────────────────────────────────────────────────┘

    Browser opens:
    ┌────────────────────────────────────────┐
    │ Sign in with Google                    │
    │                                        │
    │ [ your-email@gmail.com ]               │
    │                                        │
    │ This app will access:                  │
    │ • View and manage spreadsheets         │
    │                                        │
    │            [ Allow ]                   │
    └────────────────────────────────────────┘
    
    After first time, no need to sign in again!

┌─────────────────────────────────────────────────────────────┐
│ STEP 7: Monitor Your Data                                   │
└─────────────────────────────────────────────────────────────┘

    Google Sheet updates every 30 seconds:
    
    ┌───────────┬─────────────┬────────┬──────┬────────┐
    │ Item ID   │ Title       │ Clicks │ CTR  │ Orders │
    ├───────────┼─────────────┼────────┼──────┼────────┤
    │ 29254231..│ d'Alba To...│ 49     │ 0.8% │ 11     │
    │ 29290001..│ Phấn Nền ...│ 46     │ 0.6% │ 13     │
    │ 29123456..│ Serum dưỡ...│ 42     │ 1.4% │ 1      │
    └───────────┴─────────────┴────────┴──────┴────────┘
    
    ✨ Data refreshes automatically!

┌─────────────────────────────────────────────────────────────┐
│ DONE! 🎉                                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## Quick Troubleshooting

```
❌ "Python not found"
   → Reinstall Python
   → ✅ Check "Add Python to PATH"
   → Restart computer

❌ "credentials.json not found"
   → Download from Google Cloud Console
   → Save in: letu-live-tracker\backend\

❌ "Permission denied" (Google Sheets)
   → Delete: backend\token.json
   → Restart application
   → Sign in again

❌ Port already in use
   → Close other applications
   → Or change port in config.py

❌ Chrome driver error
   → Install Google Chrome browser
   → Restart application
```

---

## Daily Workflow

```
Morning:
  1. Double-click start.bat
  2. Wait for browser to open
  
During Livestream:
  3. Enter Session ID
  4. Enter Google Sheet URL
  5. Click "Start Tracking"
  6. Monitor Google Sheet
  
End of Day:
  7. Click "Stop Tracking"
  8. Export data (optional)
  9. Close windows
```

---

## File Locations

```
letu-live-tracker/
│
├── 🚀 START HERE
│   ├── setup.bat          ← Run first (one-time)
│   └── start.bat          ← Run daily
│
├── 📖 READ THESE
│   ├── QUICKSTART.md      ← 5-minute guide
│   ├── WINDOWS_INSTALLATION.md
│   └── GOOGLE_SHEETS_SETUP.md
│
└── ⚙️ IMPORTANT FILES
    └── backend/
        └── credentials.json  ← Download from Google
```

---

## Visual System Overview

```
┌──────────┐
│   YOU    │
│ (Windows)│
└────┬─────┘
     │
     │ 1. Enter Session ID + Sheet URL
     ▼
┌──────────────────┐
│  React Frontend  │  ← http://localhost:5173
│  (Port 5173)     │
└────┬─────────────┘
     │
     │ 2. API Request
     ▼
┌──────────────────┐
│  Flask Backend   │  ← http://localhost:5000
│  (Port 5000)     │
└────┬─────┬───────┘
     │     │
     │     │ 3a. Scrape        3b. Update
     ▼     ▼
┌─────────┐  ┌──────────────┐
│ Shopee  │  │ Google Sheet │
│Dashboard│  │ (Your Sheet) │
└─────────┘  └──────────────┘
     │              ▲
     │              │
     └──────────────┘
     4. Data flows automatically
        every 30 seconds
```

---

## What Each Component Does

```
┌──────────────────────────────────────────────────────┐
│ Frontend (React)                                      │
│ • Shows beautiful UI                                 │
│ • Gets your inputs                                   │
│ • Sends to backend                                   │
│ • Shows status updates                               │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ Backend (Python)                                      │
│ • Receives requests                                  │
│ • Opens Shopee dashboard in background               │
│ • Extracts product data                              │
│ • Updates Google Sheet                               │
│ • Repeats every 30 seconds                           │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ Google Sheets                                         │
│ • Stores all your data                               │
│ • Updates automatically                              │
│ • You can view/edit/share anytime                    │
│ • Data persists forever                              │
└──────────────────────────────────────────────────────┘
```

---

## Success Indicators

```
✅ Everything is working when you see:

1. Two command windows open
   ┌──────────────┐  ┌──────────────┐
   │ Backend: OK  │  │ Frontend: OK │
   └──────────────┘  └──────────────┘

2. Browser shows the UI
   🌐 http://localhost:5173
   
3. After clicking "Start Tracking":
   • Green "Tracking active" message
   • Pulse animation
   
4. In Google Sheet:
   • Headers appear (blue background)
   • Data rows fill in
   • Timestamp updates every 30s
```

---

## Need Help?

```
📚 Documentation:
   • QUICKSTART.md        - Fast setup
   • README.md            - Full guide
   • EXAMPLES.md          - Usage examples
   
🔧 Testing:
   • backend/test_scraper.py   - Test scraping
   • backend/test_sheets.py    - Test Google Sheets
   
💬 Check These:
   • Error messages in command windows
   • Browser console (Press F12)
   • Google Sheet permissions
```

---

## Remember

```
💡 Keep both command windows open while tracking
💡 Don't close browser tab during tracking
💡 First time needs Google sign-in (only once)
💡 Data updates every 30 seconds automatically
💡 Each session can have its own Google Sheet
```

---

🎉 **You're all set! Start tracking your livestreams!** 🎉
